{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for the Mirror Agent MVP.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
